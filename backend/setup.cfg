[nosetests]
match = ^test
nocapture = 1
cover-package = backend
with-coverage = 1
cover-erase = 1

[compile_catalog]
directory = backend/locale
domain = backend
statistics = true

[extract_messages]
add_comments = TRANSLATORS:
output_file = backend/locale/backend.pot
width = 80

[init_catalog]
domain = backend
input_file = backend/locale/backend.pot
output_dir = backend/locale

[update_catalog]
domain = backend
input_file = backend/locale/backend.pot
output_dir = backend/locale
previous = true
